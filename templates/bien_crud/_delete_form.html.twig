<form method="post" action="{{ path('app_bien_crud_delete', {'id': bien.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer ce bien ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien.id) }}">
	<button class="btn">Delete</button>
</form>
